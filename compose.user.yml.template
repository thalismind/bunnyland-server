# Generated by scripts/vps-docker-setup.
#
# This is the only deployment-specific Compose file. After setup, edit compose.user.yml
# to change the domain, data directory, image tags, runtime settings, Ollama key,
# Discord token, favicon, homepage mount, or world save.
# Keep compose.user.yml private; it contains deployment secrets.

services:
__SERVER_SERVICE__
__FRONTEND_SERVICE__
