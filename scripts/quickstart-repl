#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_dir"
exec uv run --extra repl --extra llm bunnyland repl \
  --generator apple-crossing \
  --llm \
  --chat-provider ollama-local \
  --chat-model qwen3.5:9b \
  "$@"
