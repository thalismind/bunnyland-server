#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
workers="${BUNNYLAND_TEST_WORKERS:-4}"

exec "$repo_dir/scripts/test-coverage" \
  -n "$workers" \
  --dist=loadfile \
  "$@"
